{
  config.home.rum.programs.helix = {
    enable = true;

    settings = {
      theme = "catppuccin_macchiato";

      editor = {
        line-number = "relative";
        auto-format = true;
        file-picker.hidden = false;
      };
    };
  };
}
